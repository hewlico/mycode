#!/usr/bin/env python3

hostname = input("What value should we set?")

if hostname.lower() == "mtg":
    if hostname == "mtg":
        print("Hostname matches mtg")
    print("Hostname is equal to MTG")
print("Exiting script")
